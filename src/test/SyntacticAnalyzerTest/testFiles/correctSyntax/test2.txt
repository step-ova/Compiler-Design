class car {
	
	int function(int array[1][2][3], int array[1][1][1]){

	};

};

program{
	
};

int elseIf(){

	if(array[idx] > maxValue) then {
		maxValue = array[idx];
	
	}
	
	else if(array[idx] > maxValue) then {
				maxValue = array[idx];
		}   else{};
		
	;
		
};

int elseIfStatementStar(){

	if(array[idx] > maxValue) then {
		maxValue = array[idx];
	
	}
	
	else {
		if(array[idx] > maxValue) then {
				maxValue = array[idx];
		}   else{};
		
		if(array[idx] > maxValue) then {
				maxValue = array[idx];
		}   else{};

		if(array[idx] > maxValue) then {
				maxValue = array[idx];
		}   else{};
	};
	
};


int getTest(int num1){
	
	get(variable.variable.variable);
	
};

int putTest(int num1, int num2){

	put(array[idx] > maxValue);
	
};

int returnTest(int num1[1][2][3]){

	return(array[idx] <> maxValue);
	
};

int assignStatTest(int num1[1][2][3], int num2[1][2][3], int num3[1][2][3]){
	
	num1 = num2 + num3;
	
};
